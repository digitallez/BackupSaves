============================================================
  Не удалось выложить релиз на GitHub
============================================================
Причина: {REASON}

Zip уже готов локально (сборка не потеряна):
  {ZIP}

Что сделать:
  1. Откройте командную строку:
       Win+R  ->  наберите: cmd  ->  Enter

  2. Если команда gh не находится — установите GitHub CLI:
       https://cli.github.com/
     После установки закройте и снова откройте окно cmd.

  3. Войдите в GitHub одной командой:
       gh auth login

     В мастере обычно выбирают:
       - GitHub.com
       - HTTPS
       - Login with a web browser
     Дальше подтвердите вход в браузере (аккаунт digitallez).

  4. Проверьте, что вход прошёл:
       gh auth status

  5. Снова запустите upload-release.bat
     (пересобирать Release не нужно — скрипт снова возьмёт bin\Release).
============================================================
